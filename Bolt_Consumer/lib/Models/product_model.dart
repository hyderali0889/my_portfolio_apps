class ProductModel {
  int id;
  String name;
  String description;
  int price;
  String label;
  String color;

  ProductModel(this.id, this.name, this.description, this.price, this.label, this.color);
}
